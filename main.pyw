from get_subject_deadlines import get_deadlines
"""
import functions from each get_subject_deadlines file to run them in the main loop
you can use the threading library to speed up the process
"""
def main():
    get_deadlines()

if __name__=='__main__':
    main()